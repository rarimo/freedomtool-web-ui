export enum SectionAnchor {
  Details = 'details',
  Criteria = 'criteria',
  Questions = 'questions',
}

export enum ProcessingPollStep {
  Initial,
  Image,
  Metadata,
  Proposal,
  QrCode,
  Indexing,
  Live,
}
